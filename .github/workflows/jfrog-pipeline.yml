name: build

on:
  push:
  pull_request:
    branches: [ main ]


env:
  DOTNET_VERSION: '6.0.401' # The .NET SDK version to use

jobs:
  build:

    name: build-${{matrix.os}}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Install dependencies
      run: dotnet restore src/Calculator.sln 

    - name: publish
      run: dotnet nuget  push nuget/AspenTech.Calculator.Sample.1.0.0.nupkg  AKCp8nzqUqDp7pJzWDK5FTvXzc8fxRyu5XwEUPGngkF2o6CYgFEaZK8wM4yqy3naqYrXDDCyC -Source http://artifactory4.rnd.aspentech.com:8081/artifactory/api/nuget/devops-nuget-dev-local/
      
    # - name: publish
    #   uses: jfrog/setup-jfrog-cli@v3
    #   env:
    #     JF_URL: ${{ secrets.JF_URL }}
    #     JF_USER: ${{ secrets.JF_USER }}
    #     JF_PASSWORD: ${{ secrets.JF_PASSWORD }}
    #     F_ACCESS_TOKEN: ${{ secrets.JF_ACCESS_TOKEN }}
    #     run: |
    #       nuget sources Add -Name ArtifactoryV2 -Source http://artifactory4.rnd.aspentech.com:8081/artifactory/api/nuget/devops-nuget-dev-local -username admin -password <PASSWORD>
